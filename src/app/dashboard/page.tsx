

export default function DashboardPage() {
  // Get user and redirect depending on role
  // Need to write logic that gets user role
  // if Not logged in go to home page?
  // if role = null or roll = user redirect ("/")
  // if role = admin redirect("/dashboard/admin")
  // if role = seller redirect("dashboard/seller")
  
  // just check roles, don't return anything. just redirect
  return (
    <div>Dashboard Page</div>
  )
}
